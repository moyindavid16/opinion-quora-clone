import Nav from "@/components/Nav";
import Providers from "@/lib/Providers";
import "./globals.css";
import {Toaster} from "@/components/ui/Toaster";
import {ReactNode} from "react";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

interface RootLayoutProps {
  children: ReactNode,
  createPostModal: ReactNode,
  createQuestionModal: ReactNode;
}

export default async function RootLayout({children, createPostModal, createQuestionModal}: RootLayoutProps) {
  return (
    <html lang="en" suppressHydrationWarning={true}>
      <body className="h-screen bg-gray-100" suppressHydrationWarning={true}>
        <Providers>
          <Nav />
          <>
            {createQuestionModal}
            {createPostModal}
            {children}
          </>
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
